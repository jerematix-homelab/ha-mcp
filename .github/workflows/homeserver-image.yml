name: Build homeserver image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  image:
    uses: jerematix-homelab/kubernetes-homeserver/.github/workflows/build-local-image.yaml@main
    with:
      dockerfile: Dockerfile.ghcr
      image: homeserver/ha-mcp
